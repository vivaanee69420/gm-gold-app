# Production builds (npm run build) target the hosted API on Railway.
# Railway's own build sets VITE_API_URL as a service variable, which takes
# precedence over this file — keep the two in sync.
VITE_API_URL=https://api-production-9d24.up.railway.app
