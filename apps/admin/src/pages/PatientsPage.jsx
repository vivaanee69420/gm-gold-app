// Placeholder so App.jsx's route table resolves. Task 11 builds the real patients list
// from `data.patients` (GET /admin/patients — raw snake_case columns).
export default function PatientsPage() {
  return null;
}
